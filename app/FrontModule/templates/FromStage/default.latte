{block title}{_'From the stage'}{/block}
{block content}
    {var $hero = $pageSections['from_stage']['hero'] ?? null}
    <section class="page-hero">
        <div class="container">
            <p class="eyebrow eyebrow--light">{$locale === 'cs' ? 'Z pódia' : 'From the stage'}</p>
            <h1>{$hero?->title ?: 'Z pódia'}</h1>
            <p>{$hero?->subtitle ?: 'Články, rozhovory, publikace a zmínky o Fourhands.'}</p>
        </div>
    </section>

    <section class="section">
        <div class="container">
            {if $publications}
                <div class="row gy-4">
                    <div class="col-lg-4" n:foreach="$publications as $publication">
                        <article class="publication-card reveal">
                            {if $publication->image_path}
                                <img src="{$basePath}/{$optimizedImage($publication->image_path, 1200)}" srcset="{$basePath}/{$optimizedImage($publication->image_path, 480)} 480w, {$basePath}/{$optimizedImage($publication->image_path, 1200)} 1200w" sizes="(max-width: 600px) 92vw, 33vw" alt="{$publication->title}" loading="lazy">
                            {/if}
                            <div class="publication-card__body">
                                <p class="eyebrow">{$publication->source ?: ($locale === 'cs' ? 'Externí zdroj' : 'External source')}</p>
                                <h2>{$publication->title}</h2>
                                <p class="publication-card__date">{$publication->publish_date|date:'j. n. Y'}</p>
                                {if $publication->short_description}
                                    <p>{$publication->short_description}</p>
                                {/if}
                                {if $publication->url}
                                    <a class="button button--outline" href="{$publication->url}" target="_blank" rel="noopener noreferrer">{_'Read more'} <span>→</span></a>
                                {/if}
                            </div>
                        </article>
                    </div>
                </div>
            {else}
                <p>{_'No publications available yet.'}</p>
            {/if}
        </div>
    </section>
{/block}
